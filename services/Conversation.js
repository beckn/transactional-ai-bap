class Conversation{
    constructor(session) {
        this.session = session;
    }

    async getResponse(message) {
        let response = {};
        
        // get answer from AI 
        
        return response;
    }
}

export default Conversation;